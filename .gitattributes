# .bat 必须 CRLF，否则 cmd 可能解析异常
*.bat text eol=crlf
# PowerShell 脚本保持 CRLF；脚本以 UTF-8 with BOM 保存（PowerShell 5.1 读中文必需），别做编码转换
*.ps1 text eol=crlf
# 文档
*.md text eol=lf
# Linux 侧脚本必须 LF：CRLF 会让 shebang 变成 "python3\r"，直接 ./xxx.py 执行报 "没有那个文件或目录"
*.py text eol=lf
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
